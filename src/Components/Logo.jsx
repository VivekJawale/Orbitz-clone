export const Logo = ({ iconColor, w, h, textColor }) => {
  return (
    <img
      className="nav-img"
      src="https://www.orbitz.com/_dms/header/logo.svg?locale=en_US&siteid=70201&2"
      alt=""
    ></img>
  );
};
